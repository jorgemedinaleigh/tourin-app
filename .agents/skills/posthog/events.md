# PostHog Event Inventory Pointer

The current PostHog event inventory now lives in `../../SKILLS.md#current-posthog-event-inventory`.

Update that section when adding, renaming, or removing `posthog.capture(...)` calls.
