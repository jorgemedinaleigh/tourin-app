# App Name Change Workflow Pointer

The canonical app name change workflow now lives in `../SKILLS.md#app-name-change-workflow`.

This file is kept only so agents that look under `.agents/workflows/` can find the current source.
